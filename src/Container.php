<?php

namespace Verja;

class Container
{
    public function getData(array $data = null)
    {
//        if ($data) {
//            $this->setData($data);
//        }
//
        $result  = [];
//        $rawData = $this->rawData;
//        foreach ($this->fields as $key => $field) {
//            $result[$key] = $field->filter(isset($rawData[$key]) ? $rawData[$key] : null);
//        }

        return $result;
    }
}
